import { Consumer } from "kafkajs";
import { MessageControl } from "../Message/MessageControl";
export declare class KafkaMessageControl implements MessageControl {
    private readonly consumer;
    private readonly topic;
    private readonly partition;
    private readonly offset;
    constructor(consumer: Consumer, topic: string, partition: number, offset: string);
    ack(): Promise<void>;
    nack(error?: Error): Promise<void>;
}
//# sourceMappingURL=KafkaMessageControl.d.ts.map