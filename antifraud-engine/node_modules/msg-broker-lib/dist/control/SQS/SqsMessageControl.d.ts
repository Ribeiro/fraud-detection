import { SQSClient } from "@aws-sdk/client-sqs";
import { MessageControl } from "../Message/MessageControl";
export declare class SqsMessageControl implements MessageControl {
    private readonly sqsClient;
    private readonly queueUrl;
    private readonly receiptHandle;
    constructor(sqsClient: SQSClient, queueUrl: string, receiptHandle: string);
    ack(): Promise<void>;
    nack(error?: Error): Promise<void>;
}
//# sourceMappingURL=SqsMessageControl.d.ts.map